# Example environment file for python-maker-bot
# Copy this to `.env` and fill in the values before running the app.

# HuggingFace API token (required for HuggingFace provider)
HF_TOKEN=your_huggingface_api_token_here

# API key for OpenAI-compatible providers (optional, only needed for openai-compatible provider)
LLM_API_KEY=your_api_key_here